# optimizer

Given an inventory (weapons, pictos, luminas, skills, attributes), enumerates build combinations, scores them using the formulas stored in the vault, and returns the top N.

Phase 3 deliverable — currently a stub. The CLI (`optimizer --inventory path.json`) lands first; the FastAPI wrapper follows.
